salário = float(input('Insira seu salário atual: R$'))
aumento = 15/100 * salário
novo_salário = salário + aumento
print('Um funcionário que ganhava R${}, com 15% de aumento, passou a ganhar R${:.2f}.'.format(salário, novo_salário))
