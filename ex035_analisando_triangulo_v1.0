"""Desenvolva um programa que leia três comprimentos e diga ao usuário
se esses comprimentos pordem formar um triângulo."""

print('-=' * 20)
print('Analisador de Triângulos')
print('-=' * 20)

r1 = float(input('Insira o primeiro segmento: '))
r2 = float(input('Insira o segundo segmento: '))
r3 = float(input('Insira o terceiro segmento: '))

if r1 < r2 + r3 and r2 < r1 + r3 and r3 < r1 + r2:
    print('Os segmentos acima PODEM FORMAR triângulo.')
else:
    print('Os segmentos acima NÃO PODEM FORMAR triângulo.')
